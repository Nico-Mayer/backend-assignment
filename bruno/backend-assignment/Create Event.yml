info:
  name: Create Event
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/events
  body:
    type: json
    data: |-
      {
        "type": "test.event",
        "payload": {
          "msg": "hello world"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
